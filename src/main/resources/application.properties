spring.application.name=schoolRegistry

# spring.config.import=file:.env[.properties],file:.env[.properties]

# Database
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# JPA
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

# Swagger
springdoc.swagger-ui.path=/swagger-ui.html

# JWT
jwt.secret=MiClaveUltraSecretaJWT123456789!
jwt.expiration=86400000
jwt.issuer=codesa.schoolRegistry
jwt.audience=school-api-users
